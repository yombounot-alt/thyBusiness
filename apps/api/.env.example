DATABASE_URL=postgresql://thybusiness:thybusiness@localhost:5434/thybusiness
REDIS_URL=redis://localhost:6380

JWT_ACCESS_SECRET=change-me-dev-access-secret
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL_DAYS=30

OTP_DRIVER=mock
OTP_TTL_MINUTES=10
OTP_MAX_ATTEMPTS=5

STORAGE_DRIVER=local
STORAGE_LOCAL_PATH=./uploads

PORT=3333
NODE_ENV=development

# Comma-separated browser origins allowed to call the API (Flutter web only). Empty = any origin in development, none in production.
CORS_ORIGINS=

